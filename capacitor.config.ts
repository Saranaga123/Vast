import { CapacitorConfig } from '@capacitor/cli';

 
const config: CapacitorConfig = {
  appId: 'com.example.lastguardian',
  appName: 'Last Guardian',
  webDir: 'dist/last-guardian',
  bundledWebRuntime: false
};
export default config;
